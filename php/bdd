CREATE TABLE avis (
    id INT AUTO_INCREMENT PRIMARY KEY,
    avis text(300) NOT NULL,
    date_avis DATE NOT NULL
);

CREATE TABLE user (
    id INT AUTO_INCREMENT PRIMARY KEY,
   psedo text(300) NOT NULL,
   role text(300) NOT NULL
);

